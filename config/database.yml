default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  url: <%= ENV["DATABASE_URL"] %>

development:
  <<: *default
  database: konkatsu_kiroku_development

test:
  <<: *default
  database: konkatsu_kiroku_test

production:
  <<: *default
  database: konkatsu_kiroku_production
  